# Attendance Salary App V18 — Hosting Ready

This folder is a static PWA. Upload the contents of this folder to any static HTTPS host (for example Netlify, Cloudflare Pages, GitHub Pages, or a similar static host).

Important: service workers/PWA installation require HTTPS (except localhost). No server-side database is required; app data stays in the browser's local storage. Use the in-app Backup/Restore feature to move data between devices.

Recommended entry file: index.html
